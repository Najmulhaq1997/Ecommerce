import PropTypes from "prop-types";
import "./ImageFood.css";

const ImageFood = ({ className = "" }) => {
  return <section className={`ses-chicken-biryani-recipe-736 ${className}`} />;
};

ImageFood.propTypes = {
  className: PropTypes.string,
};

export default ImageFood;
